# 访客统计维护说明

上线版本：20260908-analytics-1。官网使用 GitHub Pages，后台继续使用现有 CloudBase 站点。

## 数据与权限

- `plutonoc-analytics-collect`：独立 HTTP 接收接口；仅 POST/OPTIONS，无查询能力。允许官网域名，单批最多 20 条、10KB；每匿名编号每分钟最多 12 批，网关提供来源 IP 时对其临时摘要限 120 批，全站最多 600 批。格式校验在数据库操作前完成。
- `plutonoc-analytics-query`：沿用原有管理员身份校验，支持 summary、visits、detail；日期使用北京时间，最近 90 天，明细 50 条/页。testOnly 仅在管理员鉴权后可用于检查测试数据，后台默认排除测试记录。
- `plutonoc_analytics_events`、`plutonoc_analytics_limits` 为 ADMINONLY 私有集合，客户端不能直接读写。只保存匿名随机编号、栏目/作品、时间、粗分设备及来源域名；不存储原始 IP 或完整来源网址。
- 页面打开/刷新分别计数；30 分钟无活动开始新浏览过程。栏目位于屏幕中间 50% 高度区域并停留 1 秒计数；大图/视频窗口打开记录作品。同一浏览过程的栏目、作品在浏览器和云端分别去重。
- 匿名编号有效 90 天；存储不可用时使用内存编号，因此刷新后的匿名人数可能增加。尊重 DNT 和 Global Privacy Control，启用这些选项时不记录。

## 性能与关闭开关

官网不加载 CloudBase SDK。主脚本后加载独立统计脚本，两帧绘制后在空闲时启动；每 10 秒合并发送，普通请求 5 秒超时，不持续重试。离开页面用 beacon 尽力发送。队列最多 100 条，超出丢弃最早待报记录。

将 `analytics-config.js` 的 `enabled` 设为 false，更新 index 中配置资源版本并发布 Pages，即可关闭上报，后台历史记录仍可查询。

## 清理与部署

`plutonoc-analytics-cleanup` 每天 04:15（北京时间）清理 90 天前事件和 1 天前限流计数。固定集合及期限，不接受调用方指定删除目标；仅接受带有私有参数的清理触发事件。单次每集合最多处理 10,000 条，需关注大流量下的过期积压及云函数超时日志。

运行 `node tools/build-analytics.mjs` 生成独立部署包，仅针对三个 analytics 函数使用 `tcb fn deploy NAME --dir work/analytics-functions/NAME --force`；不要执行全量函数部署或修改 publisher 凭据。清理包中的 cleanup-secret.json 是私有触发参数，保留现有文件；换机器部署时须安全恢复并核对定时任务 CustomArgument，不能随意重新生成后只更新函数。work/ 被 Git 忽略，禁止上传其测试脚本、凭据和私有清理参数。

后台通过 `tools/deploy-admin-cloudbase.ps1` 同步 admin-analytics.js。发布后逐一比对后台 HTML/JS/CSS，再开启官网开关并推送 main；发布前核对远端以保留后台新内容。

## 2026-09-08 验证记录

- 19 项 Node 测试通过，覆盖现有图片加载及统计的去重、刷新、会话切换、存储失败、请求超时、容量限制、开关、栏目停留和窗口缩放。语法、内容一致性、首屏资源构建及静态校验通过。
- 云端实测：相同批次 12 次提交只存一条 page；第 13 批返回 429，另一匿名编号仍返回 204；无效/超大请求返回 400/413，其他来源 403，GET 405。
- 管理员查询通过；无身份云函数调用 FORBIDDEN，SDK 退出后的查询被拒绝。两个集合的 ADMINONLY 权限已重新核对。
- 使用 52 条明确标记的分页测试访问，第一页 50 条、第二页有剩余记录且不重叠。测试记录默认不进入正式统计。
- 清理实测：91 天前测试事件和 2 天前限流记录被删除，近期记录保留。每日触发器处于启用状态，私有参数与部署包一致。
- 本地真实浏览器使用测试配置和仅本机的转发接口，成功产生 page、section、photo work 记录，云端管理员可读取，正式查询排除这些记录。此项证明浏览器脚本至云端的记录链路，不等同于官网跨域浏览器验收。
- 后台本地浏览器以真实云端测试结果作为界面数据，检查了桌面及 390px 布局、日期预设和展开过程；查询未完成时退出后，页面停留在登录页，统计和访问列表内容保持为空。
- 线上后台 HTML、admin.js、admin-analytics.js 已通过 HTTP 与本地文件逐字节比对。线上浏览器导航多次超时，线上管理员的完整浏览器操作、真实手机触控及普通鼠标悬停性能测试列为未完成；不能据此宣称全部实机验收通过。
- 本地加载比较只作有限样本：同一浏览器热缓存、相同窗口，关闭/开启统计的 FCP 分别为 68/124ms，动画帧间隔 P95 均为 10.1ms，均无长任务。样本量不足以判断 FCP 差异是否由统计引起，不能据此宣称性能提升或全部无回退。统计脚本没有鼠标移动处理器，也不修改原有首页展开逻辑；缺少可靠的普通鼠标移动工具，未测量其悬停开始延迟。
