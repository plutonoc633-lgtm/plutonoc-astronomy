# PlutonoC 真机验收表

本表用于人工验证真实移动浏览器。站点不会收集设备、IP、错误或行为数据；请只记录系统与浏览器版本、结果及可复现步骤。

## 测试环境

| 环境 | 设备 / 系统版本 | 浏览器 / 微信版本 | 日期 | 结果 |
| --- | --- | --- | --- | --- |
| iPhone Safari | 待填写 | 待填写 | 待填写 | 待测 |
| iPhone 微信内置浏览器 | 待填写 | 待填写 | 待填写 | 待测 |
| Android Chrome | 待填写 | 待填写 | 待填写 | 待测 |
| Android 微信内置浏览器 | 待填写 | 待填写 | 待填写 | 待测 |

## 每个环境都要检查

- [ ] 首次打开 `https://plutonoc.cn/`：珠峰、手写口号、水印和页头正常，无白屏、跳位或横向滚动。
- [ ] 慢速和快速上下滚动：内容进出场自然，返回时能重播，无明显卡顿、闪烁或错位。
- [ ] 首页五栏连续横向拖动：宽度稳定、只高亮一项，页面纵向滚动不被锁死。
- [ ] 进入摄影作品：图片分批出现，无长期空白；筛选、拖动、惯性和当前作品计数正常。
- [ ] 打开摄影灯箱：上一张、下一张、滑动和关闭正常；关闭后可继续滚动。
- [ ] 进入动态影像：当前视频封面及排序正确；视频可播放、暂停、全屏和退出，关闭后声音停止。
- [ ] 打开 INDEX 并进入各栏目；浏览器前进、后退和站内导航均定位正确。
- [ ] 新窗口直接打开 `https://plutonoc.cn/#films` 可进入动态影像；在该页刷新会按既定规则回到首页。
- [ ] 进入结尾：照片、标题、三个社交入口和底栏不重叠；链接及 `BACK TO TOP` 正常。
- [ ] 横屏再转回竖屏：布局恢复、弹窗可关闭，顶部/底部安全区没有遮挡。
- [ ] 系统启用“减少动态效果”后：内容直接显示，核心交互仍正常。

## 问题记录

出现问题时请填写：环境、页面栏目、复现步骤、预期结果、实际结果，并附截图或录屏。只有能够稳定复现的问题才进入代码修复，避免改变已确认的视觉设计。


## 2026-09-05 加载修复验证

- 新增 `image-loader.js`：每次图片加载/解码最多 12 秒，失败自动重试一次，支持取消。画布与目录不再保留无法重试的失败条目；手动“重新加载”可恢复失败图片。
- 画布先显示已有 640px 缩略图，再升级为原有高清预览；并发、DPR、缓存预算及最终构图保持原设定。大图先显示预览，切换/关闭会取消旧请求，旧响应不会覆盖当前作品。
- 首页与静态文字默认可见，动画观察器接管后才启用隐藏状态。分类封面提前约 600px 加载。画布上方常驻拖动/点击说明，并关联读屏描述；触屏提示左右拖动以保留纵向滚页。
- 没有独立短预览的视频不再悬停请求正式 MP4；首页木星视频仅在交互时请求，离开后停止并移除资源地址。
- 11 项自动测试通过，已加入 Pages 工作流：超时、解码超时、重试、取消、队列恢复、缩略图优先、缓存预算、大图异步竞态与失败保留预览。
- 本地浏览器检查通过：桌面画布出图/提示、键盘打开作品、快速切换并关闭、目录打开；390px 窄屏无横向溢出，目录为两列并显示两个原生筛选框。画布与目录注入 503 后可通过重试恢复。未操作时正式视频没有 src，受控加载记录中 MP4 请求数为 0。
- 本地受控对照（不能作为实际访客网速承诺）：人为延迟图库脚本 7 秒，旧版背景在约 7.45 秒可见，新版约 0.27 秒；在每图片约 100KB/s、额外延迟 150ms 的独立测试中，旧版首张画布图约 1.77 秒，新版约 1.33 秒，并确认后续替换为 1600px 预览。对照均为无缓存测试页面，测试代码仅在本机工作目录，未打包进网站。
- 未完成：iOS/Android/微信真机、真实触摸拖动手感及真实运营商线路复测。窄屏浏览器检查不等于真机验收。CloudBase 大视频 Range 响应仍需另行处理。
